import 'package:flutter/material.dart';

/// ألوان التطبيق — مأخوذة من تصميم منصة مزايدة
class AppColors {
  AppColors._();

  // الأساسية
  static const Color primary = Color(0xFF215744);
  static const Color background = Color(0xFFF4F5F9);
  static const Color white = Color(0xFFFFFFFF);

  // النصوص
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6F76);
  static const Color textHint = Color(0xFF9A9DA3);

  // الحدود
  static const Color border = Color(0xFFE3E5EA);
  static const Color borderStrong = Color(0xFFD5D8DE);

  // دلالية — النجاح
  static const Color success = Color(0xFF0F6E56);
  static const Color successBg = Color(0xFFE1F5EE);

  // دلالية — التحذير
  static const Color warning = Color(0xFF854F0B);
  static const Color warningBg = Color(0xFFFAEEDA);

  // دلالية — الخطر
  static const Color danger = Color(0xFFA32D2D);
  static const Color dangerBg = Color(0xFFFCEBEB);

  // دلالية — المعلومات
  static const Color info = Color(0xFF185FA5);
  static const Color infoBg = Color(0xFFE6F1FB);

  // محايد
  static const Color neutral = Color(0xFF5F5E5A);
  static const Color neutralBg = Color(0xFFF1EFE8);
}
